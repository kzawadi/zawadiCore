import 'package:designsys/designsys.dart';

class StartUpViewLandscape extends StatelessWidget {
  const StartUpViewLandscape({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("mobile landscape"),
        color: Colors.redAccent,
      ),
    );
  }
}

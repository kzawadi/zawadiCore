import 'package:designsys/designsys.dart';

class StartUpMobilePortrait extends StatelessWidget {
  const StartUpMobilePortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("mobile portrait"),
        color: Colors.amber,
      ),
    );
  }
}

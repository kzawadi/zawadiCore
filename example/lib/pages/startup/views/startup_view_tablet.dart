import 'package:designsys/designsys.dart';

class StartUpViewTablet extends StatelessWidget {
  const StartUpViewTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Tablet"),
        color: Colors.greenAccent,
      ),
    );
  }
}
